# Build outputs — large, regenerated by the pipeline
julia_ecosystem.nq
julia_ecosystem_smoke.nq
out/
out_smoke/
out_build.log

# Scratch / recovery artifacts
webspike/
_salvage/

# Notebook render artifacts (regenerated by `quarto render`)
notebook/*.js
notebook/ecosystem_insights_files/
notebook/.quarto/

# NOTE: web/data.nq.gz IS committed — it is the published snapshot the
# Pages explorer serves. web/web.js and web/web_bg.wasm (vendored Oxigraph)
# are committed too.
